# AI 여행 교육 프로젝트 작업 규칙

이 문서는 `AI와 함께 떠나는 스마트 여행 생활` 프로젝트에서 Codex가 작업할 때 따라야 하는 기본 규칙입니다.

## 1. 계획 요청이 있을 때

사용자가 다음과 같이 말하면 바로 코딩하지 않습니다.

- "계획해줘"
- "계획부터 알려줘"
- "바로 코딩하지 말고"
- "먼저 검토해줘"

이 경우 Codex는 먼저 작업 계획만 제시하고, 사용자의 승인 후 수정 작업을 시작합니다.

## 2. 단순 수정 요청일 때

사용자가 다음과 같이 말하면 바로 수정해도 됩니다.

- "수정해줘"
- "바꿔줘"
- "반영해줘"
- "적용해줘"

이 경우 Codex는 파일을 수정하고, 수정 결과를 검토한 뒤 사용자에게 알려줍니다.

## 3. 모든 작업의 GitHub push와 Firebase 배포

`AI_travel_edu`에서 만드는 모든 자료와 기능은 로컬 수정만으로 완료된 것으로 보지 않습니다.

사용자가 요청한 작업의 최종 완료 시점에는 별도의 추가 확인 없이 반드시 다음 순서로 진행합니다.

1. 변경 범위와 기존 콘텐츠 보호 여부 확인
2. 빌드 및 검증
3. Git 커밋
4. GitHub push
5. Firebase Hosting 배포
6. 배포 성공 여부와 배포 링크 확인

화면 구성이나 문구를 여러 차례 조정하는 동안에는 중간 결과를 반복 배포하지 않습니다. 사용자의 요청사항이 최종 반영되고 검증이 끝난 시점에 한 번만 push와 배포를 진행합니다.

GitHub 인증, Firebase 인증, 네트워크 또는 필수 도구 문제로 게시할 수 없으면 작업을 완료했다고 보고하지 않고, 게시 대기 상태와 필요한 조치를 사용자에게 알립니다.
## 4. Firebase 데이터 보호

기존 Firebase 저장 데이터가 초기화되거나 삭제될 수 있는 작업은 임의로 하지 않습니다.

Firestore, Storage, Hosting 설정을 변경할 때는 기존 수강생 제출 데이터와 첨부 이미지가 유지되는지 먼저 확인합니다.

## 5. 프로젝트 개발 기준

- HTML, CSS, JavaScript 중심으로 작업합니다.
- 프레임워크를 새로 도입하지 않습니다.
- 모바일 우선으로 구성합니다.
- 초등학생·일반인·시니어·전문직 등 다양한 수강생과 스마트폰 사용 환경을 고려해 읽기 쉬운 글씨, 누르기 쉬운 버튼, 명확한 문장, 단순한 화면을 유지합니다.
- 디자인은 주황색·베이지 계열의 "여행가의 노트" 느낌을 기본으로 합니다.
- 교재 원본 이미지를 통째로 삽입하지 않고, 필요한 경우 화면 일부나 버튼 참고 이미지 형태로 정리합니다.

## 6. 주차별 내용 보호

- 1주차는 `AI와 여행지·여행 계획` 수업입니다.
- 2주차는 카메라 기능과 설정 수업입니다.
- 3주차는 갤러리앱과 사진 정리 수업입니다.

3주차 내용을 카메라 수업으로 바꾸지 않습니다.

## 7. 관리자와 제출 화면 주의사항

- 기관 선택 표시에는 "1주차 시작" 같은 문구를 붙이지 않습니다.
- 기관명만 보여줍니다.
- 기관별 시작 주차는 관리자 설정값으로만 관리합니다.
- 후기 제출 화면은 수강생이 QR로 접속하는 것을 기준으로 단순하게 유지합니다.

## 8. 클린 아키텍처와 작업 경계

- 프로젝트의 소스 오브 트루스는 `src`, `content`, `config`입니다.
- 공개 경로의 HTML, CSS, JavaScript는 빌드 결과물이므로 직접 수정하지 않습니다.
- 포털, 스마트 여행 정규과정, AI 그림동화책 정규과정, 특별과정은 서로 독립된 컨텍스트로 관리합니다.
- 한 컨텍스트의 수정이 다른 컨텍스트의 파일을 변경하지 않도록 합니다.
- 새 강의안은 기존 강의 파일에 덧붙이지 않고 새 컨텍스트 또는 새 주차 소스로 추가합니다.
- 작업 전 `content/catalog.json`과 `config/project.manifest.json`으로 수정 범위를 먼저 확인합니다.
- `.netlify`, 캐시, 생성 결과물은 분석의 기준 파일로 사용하지 않습니다.

## 9. 강의 원문 보호

- 강의 문구는 `locks/content-lock.json`의 기준선으로 보호합니다.
- 레이아웃 수정 중 제목, 본문, 과제, 예시 문장을 임의로 요약하거나 바꾸지 않습니다.
- 사용자가 문구 변경을 요청한 경우에만 해당 컨텍스트의 원문과 잠금 파일을 함께 갱신합니다.
- 빌드 전에 원문 변경 내역을 확인하고, 빌드 후 `npm run validate`를 통과해야 합니다.
- 기존 주차를 수정하는 작업과 새 주차를 추가하는 작업을 분리합니다.

## 10. 빔프로젝터 강의 화면 규칙

- 강의용 슬라이드는 16:9 비율을 유지합니다.
- 제목은 50px 이상, 부제목은 34px 이상, 본문은 28px 이상, 보조 설명은 22px 이상을 기본으로 합니다.
- 내용이 넘칠 때 글씨를 작게 줄이지 않고 페이지 분리 또는 레이아웃 재배치로 해결합니다.
- 제목, 부제목, 본문, 페이지 표시와 조작 버튼의 위치는 공통 토큰으로 관리합니다.
- 전체화면과 일반화면 모두 페이지 이동 버튼의 대비와 가시성을 유지합니다.

## 11. 빌드·검증·배포 순서

1. 해당 컨텍스트의 소스만 수정합니다.
2. 승인된 문구 변경이면 콘텐츠 잠금을 갱신합니다.
3. `npm run build`로 공개 결과물을 생성합니다.
4. `npm run validate`로 원문, 출력 일치, 경계, 프로젝터 규격을 확인합니다.
5. 검증 성공 후에만 GitHub push와 Firebase Hosting 배포를 진행합니다.

Firebase 배포에서는 `src`, `content`, `config`, `locks`, `tools`, `docs`를 공개하지 않습니다.

## 12. 전체 교육과정의 강의 흐름 설계 및 검증

이 규칙은 스마트 여행·AI 그림동화책 등 모든 정규과정, 모든 특강, 앞으로 추가하는 새 교육과정에 공통으로 적용합니다.

### 12.1 교육 설계의 기본 순서

- 강의는 원칙적으로 `목표 안내 → 필수 개념·안전 원칙 → 설명 또는 시범 → 입력 예시 → 단계별 실습 → 결과 확인·수정 → 응용·공유`의 흐름으로 구성합니다.
- 주제에 필요하지 않은 단계는 생략할 수 있지만, 필요한 이론이나 설명 없이 실습부터 시작하지 않습니다.
- 각 실습은 앞 단계에서 만든 결과를 이어서 사용하게 구성하고, 같은 정보나 결과물을 반복해서 입력하게 만들지 않습니다.
- 이력서와 자기소개서처럼 결과물이 연결되는 활동은 사실·경력·성과를 먼저 확정한 뒤 다음 문서로 확장합니다.

### 12.2 예시와 실습 화면

- 처음 접하는 프롬프트나 활동에는 수강생이 따라 할 수 있는 입력 예시를 필요한 위치에 배치합니다.
- 예시는 완성된 문장만 강요하지 않고 단어 나열, 짧은 표현, 짧은 문장도 함께 보여줍니다.
- 예시 화면은 별도 요청이 없으면 애니메이션 없이 한 번에 보여주어 불필요한 클릭을 줄입니다.
- 단계 카드가 많을 때는 카드 하나씩이 아니라 의미가 연결된 한 행 또는 한 묶음씩 표시합니다.

### 12.3 다양한 수강생과 사용 기기 기준

- 수강생의 연령이나 직업을 임의로 한정하지 않고 초등학생·일반인·시니어·전문직 등 해당 과정의 실제 대상에 맞춰 설명 수준과 예시를 조정합니다.
- 스마트폰 사용 비중이 높은 점을 우선 고려하되 컴퓨터에서도 같은 내용과 기능을 사용할 수 있게 구성합니다.
- 작은 스마트폰 화면에서도 쉽게 따라갈 수 있도록 한 번에 한 질문만 제시합니다.
- 단어 또는 짧은 표현으로도 답할 수 있음을 안내합니다.
- 답변이 짧아도 AI가 경력·수치·사실을 추측해서 만들지 않도록 프롬프트에 명시합니다.
- 지나치게 긴 프롬프트, 반복 입력, 불필요한 화면 이동과 클릭을 줄입니다.
- 앞 단계의 결과를 같은 대화방에서 다음 단계에 이어서 사용할 수 있도록 안내합니다.

### 12.4 순서 변경 전 필수 점검

- 페이지를 추가·삭제·이동하거나 단계 순서를 바꾸는 요청은 일부 화면만 보고 바로 구현하지 않습니다.
- 먼저 전체 과정에서 해당 화면의 선행 학습, 입력값, 생성 결과, 다음 사용처를 확인합니다.
- 순서 변경안을 사용자에게 먼저 제시하고 승인받은 뒤 구현합니다.
- 한 단계의 이동이 이후 페이지 번호, 안내 문구, 단계표, QR 후기 화면에 미치는 영향을 먼저 웹에서 확인합니다.

### 12.5 완료 전 순서 일치 검증

웹 수정 단계에서는 다음 세 가지 순서가 일치하지 않으면 작업을 완료한 것으로 보고하지 않습니다.

1. 화면에 표시된 단계 번호와 강의 순서
2. 이전·다음 버튼으로 이동하는 실제 발표 순서
3. JavaScript가 재배치한 DOM 슬라이드 순서

- 프롬프트의 결과물이 실제로 다음 단계에서 사용되는지 확인합니다.
- 앞에서 만든 결과를 뒤늦게 다시 반영하느라 같은 문서를 반복 수정하는 역순 구조가 없는지 확인합니다.
- HTML 구조, 중복 ID, 복사 버튼 연결, 페이지 수, 단계 번호를 정적으로 검증합니다.
- 사이트와 PDF를 함께 제공하는 과정은 먼저 웹 수정과 검증을 모두 끝내고 사용자의 최종 승인을 받습니다.

### 12.6 PDF 생성 비용과 중복 작업 방지

- 문구·순서·디자인을 조정하는 중간 단계에서는 PDF를 생성하거나 반복 갱신하지 않습니다.
- 사용자가 웹 화면의 최종 완료를 승인한 뒤 승인된 웹 결과를 기준으로 PDF를 한 번 생성합니다.
- PDF 생성 후에만 웹의 최종 내용·순서와 PDF의 페이지 내용·순서를 한 번 대조합니다.
- PDF에서 문제가 발견되면 웹 원본을 먼저 수정하고 검증한 뒤 PDF를 다시 생성합니다. PDF만 따로 고쳐 웹과 내용이 달라지게 하지 않습니다.
- 사용자가 중간 PDF 미리보기를 명시적으로 요청하지 않는 한 중간 PDF 렌더링과 전체 페이지 이미지 검사를 수행하지 않습니다.
- PDF 검증은 전체 페이지를 반복 렌더링하지 않고 변경 페이지, 첫 페이지, 마지막 페이지, 페이지 수와 규격을 중심으로 최소화합니다.
